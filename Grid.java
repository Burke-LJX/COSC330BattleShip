public class Grid {
    Tile[][] GameBoard = new Tile[10][10];


    public void setState(Grid g) {

    }
    //getTileState()
    //update() - send updates player info
    //possibly interactable state - based on whether player's turn

    //Self Grid = Child class of grid
    /*store self ship locations
     * setShipLocation(); - also have random option
     * check legal placement of ships;
     * 
     * Enemy Grid = child class of grid
     * track player attacks
     * boolean hit/miss - updates graphics
     * check if title has already been attacked
     */
}